# THE BLACK LEDGER — SPEC v7.1 · TRUE COUNT (CLOSING AMENDMENT)
## Final synthesis. Co-binding with v7; where they conflict, v7.1 wins. After this document, no further concept changes: every subsequent session is execution polish. Tier C (the typographic build) remains inviolable as the no-GL fallback.

# §0 — AMENDMENTS TO LAW
0.1 STRUCK sitewide and from code: all particle/dust systems, every prior world-dressing remnant.
0.2 ART DIRECTION RESTORED: data governs PROPORTION (counts, sizes, ratios are computed from claims.csv and never faked); a designed eye governs the IMAGE. Every major view has an authored keyframe — camera position, light, composition — specified below and recorded in the Decision Ledger as "authored."
0.3 THE AUDACITY CLAUSE (amends the Fusion Law): one class of choice is exempt from dual-justification — an image is permitted to exist because it is TRUE, even if it serves no navigational function. Truth at honest scale is the site's spectacle. Logged in the Ledger as "audacity: true count."

# §1 — THE MASS (the image of the site)
1.1 The subject rendered at true count: the admitted FY2025 black budget = 1,011,000,000 hundred-dollar bills (C-0013 ÷ 100). Rendered as a real instanced mass on the GPU — WebGPU instancing with LOD tiers (near: individual modeled bills with worn-edge normal maps; mid: instanced stacks of 100; far: volumetric brick aggregates). The count is never decorative — bill count, stack count, and aggregate volumes derive live from claims.csv; a wrong count is a build failure.
1.2 Palette unchanged (ground/bone/dim/hairline/ember). Bills render in bone-on-ground monochrome — this is a ledger, not a casino; ember remains reserved (live accrual + focus).
1.3 The authored keyframes (§0.2), the site's clippable shots:
 K1 One bill, flat, alone, lit hard from one side, on black. Caption: `One hundred dollars.`
 K2 The admitted mass at full scale, hanging in the void, lit like evidence. Caption: `$101.1 billion. Two published numbers. Nothing beneath them.`
 K3 Beside it, the unreported mass (C-0009 ÷ 100 = 2,069,218,711 bills) — visibly twice the size. Caption: `And this is what the government's own tracking was never told about.`
 K4 Seven bills and change, extracted, laid flat before the camera. Caption: `Your household's share this year.` (C-0028 math)
 K5 The estimate pile (C-0024): a mass rendered permanently out of focus — sharpness-as-confidence (v7 §3) made physical. It never resolves. `An estimate. It does not sharpen.`

# §2 — THE PULLBACK (the spine; replaces the plane as the primary journey)
2.1 One continuous scroll-scrubbed camera move: K1 → the bill joins a stack of 100 → a block of 10,000 → a pallet → the full admitted mass (K2) → the unreported mass enters frame (K3) → the residual context (v7 §4 arithmetic plays against the masses: the traceable mass removes itself; what remains stands) → Your Share extraction (K4) → the audit and estimate figures (K5, the 8-tally stamp) → settle on THE RECORD threshold.
2.2 Scale is honest at every frame: on-screen mono scale-line always shows current magnification and count in frame (`IN FRAME: 4,200,000 BILLS · $420,000,000`). The scale-line is the navigation — tapping it opens the v7 semantic-zoom instrument (the plane, demoted to L3/L4 method-and-record views, where the copy, computations, stamps, and provenance live exactly as specced).
2.3 Every v4 monument figure, caption, and descent copy block appears at its correct beat of the pullback, verbatim; strata epistemology intact as zoom levels within each beat.

# §3 — THE LIVE MASS (the counter, physical)
3.1 32 bills land on the admitted mass per second ($3,206/sec ÷ 100), individually visible at near/mid LOD, aggregated at far. Runs whenever the tab is visible; on return from blur, the accumulated arrears land in a 2-second cascade with the caption `+$412,368 while you were away` (computed, tabular).
3.2 This replaces every prior "Pulse" implementation; AU/HP hooks (tick, haptic) bind to landings, off-by-default sound rules unchanged.

# §4 — MOBILE, PERFORMANCE, PROOF
4.1 Mobile-first framing for all keyframes (portrait crops authored, thumb-driven scrub); 45fps floor mid-Android at every beat — evidence per phase; LOD budgets tuned until true.
4.2 Fallback ladder: Tier B (WebGL2) uses reduced LOD counts with identical true totals; Tier C untouched.
4.3 All standing tripwires + new: (r) count-verification script proving on-screen instance math equals claims.csv values at three beats, output pasted; (s) K1–K5 screenshots (mobile + desktop) in the report; (t) blur-return cascade demonstrated.

# §5 — PHASES (gated; standing session rules verbatim)
P1 Mass rendering core: instancing/LOD, K1–K2 authored, counts verified. P2 The pullback spine with copy beats. P3 K3–K5 + arithmetic performances against the masses. P4 Live mass + blur-return. P5 Demoted plane wired at L3/L4; polish pass under v4.1 micro-law. P6 PROOF per §4.3 plus full regression (Tier C, /record, files, 404, print).

## PROMPT ADDENDUM — paste with the standing rules
`SPEC-v7.1-TRUE-COUNT.md` is the closing concept amendment; move to `docs/` in Phase 0 (Haiku commit) and execute P1–P6 under the standing gates, git/deploy rules, and anti-drift rules. §0.1's cuts are executed in P1. After this spec, propose no concept changes: deviations may only adapt implementation, never the image. All captions above are verbatim; the K3 caption and §1.3 K5 line count as licensed lines already approved.
